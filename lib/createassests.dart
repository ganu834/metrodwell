import 'package:flutter/material.dart';

class Createassests extends StatelessWidget {
  const Createassests({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Create Assests"),
      ),
    );
  }
}
